# --- Core LLM / multimodal ---
# All Gemini calls (Actor, Critic, Judge, embeddings) go through LangChain —
# see langchain-google-genai below. The raw google-genai SDK is no longer imported
# directly; it is pulled in transitively by langchain-google-genai.

# --- Orchestration ---
langgraph>=0.2.0              # stateful Actor-Critic graph
langchain-core>=0.3.0        # messages (System/Human), runnables
langchain-google-genai>=2.0.0   # ChatGoogleGenerativeAI + GoogleGenerativeAIEmbeddings
langchain-text-splitters>=0.3.0   # recursive chunking

# --- RAG / vector store (local, reproducible) ---
chromadb>=0.5.0
pypdf>=4.0.0                  # PDF text extraction
# sentence-transformers>=3.0.0  # optional: local embedding fallback for offline dev
#   (not used by default — embeddings now come from Gemini, see src/llm/embeddings.py)

# --- Evaluation ---
ragas>=0.2.0                 # faithfulness / context precision (uses the LangChain Gemini LLM above)
scikit-learn>=1.5.0          # Cohen's kappa, precision/recall
statsmodels>=0.14.0          # Fleiss' kappa (3+ raters); McNemar's paired A-vs-B test
numpy>=1.26.0                # bootstrap confidence intervals in run_benchmark
pandas>=2.2.0
matplotlib>=3.9.0

# --- Demo / serving ---
fastapi>=0.115.0
uvicorn>=0.30.0
streamlit>=1.39.0
python-dotenv>=1.0.0
pillow>=10.0.0               # image loading
